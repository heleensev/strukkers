
class partucularbuilder:

    def __init__(self, inputstr):
        self._s_value = self._find_s()
        #define a sequence part_gen_sol

    def _find_s(self):
        #if somewhere x^n, then s = x
        #else: s=1

    def _check_s(self):
        #multiplicity_s = ''
        #if s in rootdict,
            #get multiplicity from root
        #part_gen_sol = s + '^' + m

    def _particular_general_sol(self):
        #check for xn^y where x>0 y>0
            #no -> add p0 => part_gen_sol += '(p_0'
            #yes -> add p0 and cunt n's
                #count n's
                #part_gen_sol += '(p_0'
                #for i in number_of_n:
                    #eq += 'p_{i} * n^{i}
            #eq += ')' + s + '^n' #close main part equation

    def _solve_part_sol(self):
        #s(n) = A * B * C where A = A if n^m, else A = 1. B = (p0+....+pi). C = s^n or 1 if s = 1

        #unsimplify so we can get things like An^2+bn+C

        #put in A * B * s^n  = n^m(p0+p1(A*B*C)+p2(ABC)+....)
        #solve



